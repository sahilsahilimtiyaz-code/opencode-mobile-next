# Managed-server captures

`light.png` and `dark.png` show synthetic Termux results inside the production
`ServersScreen`. **Check status** is exercised through the real health widget.
**Open setup controls** retains its real `/termux-setup` navigation route;
it is not a decorative no-op button in a fabricated host page.

Regenerate with `tool/capture/managed_server_health_test.dart`. The version,
process state and storage counts come from a mocked native channel. These
images do not prove Termux installation, bridge timing, authenticated server
health, crash recovery or operation on an installed APK.

Phone-gallery caption: **Synthetic Termux data in the production Servers screen**.
