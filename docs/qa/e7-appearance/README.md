# E7 appearance and Settings journey

Finish line: Settings and its owned detail pages are localized and direction-aware; Appearance previews actual theme components, keeps the current setting unchanged while browsing, and applies only on an explicit save with recoverable failure.

Non-goals: new palettes, fonts, quota integrations, provider changes, locale persistence (core owner), or a new design system.

Findings: SETTINGS-09 (swatches cannot explain text, code, controls and surfaces); E7 (app-authored copy and layout must support Arabic).

Implementation and verification in progress. Evidence is appended at the completed slice boundary.
