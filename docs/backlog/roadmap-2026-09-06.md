# Execution order and evidence guardrails — 2026-09-06

Historical ordering and acceptance guidance. For current implementation and
remaining work, use the [September 7 delivery ledger](current-state-2026-09-07.md).
Descriptions below of missing or uncommitted features reflect September 6.

Lead revision after reviewing the session against the working tree at
`84e1e0f` plus the iOS/Claude-availability follow-up. This is the ordering entry point for the
[epic/story inventory](full-backlog-2026-09-06.md) and
[innovation inventory](innovation-2026-09-06.md). Existing BE/FE IDs and
[cycle evidence](cycles.md) remain intact. Recommendations are not release
approval, implementation claims, or permission to narrow the requested scope.

## What is real today

- The app already contains dual OpenCode adapters, paginated history, request
  handling, draft recovery, usage/cost statistics, local dictation, and guided
  Termux setup. Verify residual gaps rather than rebuilding these features.
- The working-tree server pin is `1.18.29` in both Dart and the embedded shell
  fallback. Its agreement/setup tests passed in the first implementation batch.
  A runtime update reported earlier is separate from an app release.
- An experimental `ios/` runner, Keychain entitlements, branded icons and
  unsigned simulator CI are in the working tree. E12 is source preparation,
  not a proven native build or a released iPhone app.
- The optional quota collector, scoped Remaining usage screen,
  provider consumption groups and iOS gating regression fix landed in
  `84e1e0f`. The follow-up disables unsupported Claude collection without
  accessing its credentials; only Codex has an active collector path.
  [Implementation evidence](../verification/provider-quota-2026-09-06.md)
  distinguishes the first clean gate from the later single assertion failure,
  its passing focused correction, and unperformed deployment. Broad tests are deferred
  while the user-requested development batch continues.
- Full voice mode, mobile plugin cards, demo mode and alternate-agent adapters
  remain proposals, not shipped features.
- A tag identifies source; it does not prove an APK exists, is signed, installs,
  or passes a device journey. The [release checklist](../v1-release-readiness.md)
  still owns that evidence.

## Remaining work — current delivery queue

The quota batch is committed source; the iOS and Claude-availability follow-up
is uncommitted. Neither establishes an installed APK or an operational
collector deployment. The existing app version and the
separately updated server version must not be conflated.

| Workstream | Implemented so far | Next concrete work |
|---|---|---|
| E13 quota | Codex core-window collector, provider switching, stale/unknown states, provider-grouped consumption; Claude unavailable with research-only parsers | Supported/permitted Claude integration before any re-enablement; GLM, MiniMax and Gemini adapters with actual units/auth/reset contracts; explicit operator setup and authorized live verification |
| E12 iOS remote control | Unsupported-platform background fix, isolated gating tests, experimental runner/Keychain entitlements/icons and simulator CI source | macOS unsigned build, actual plugin/Keychain/privacy integration proof, then device/TestFlight gates |
| E5 input durability | Ordinary drafts use a vault; opt-in stash metadata/migration backend and synthetic tests are authored, but the live stash writer is still inline | Wire async recovery/listing and profile deletion before activating the isolated stash vault; verify focused migration/race fixtures ([groundwork](../verification/stash-attachments-2026-09-06.md)) |
| F1 voice | Existing local dictation only | Read-aloud engine/audio-focus slice, then reviewed conversational input/output and interruption handling |
| F8 onboarding | Plan and existing fixture machinery | Isolated offline demo through real UI, with no real-profile/network side effects |
| F9 connectivity | HTTPS/loopback policy and revised design | Tested private-HTTPS/tailnet/tunnel setup guidance, reachable/error diagnosis, optional discovery |
| F2 / E3 / E4 integrations | Existing catalogs and key/OAuth/MCP flows | Read-only plugin inspection, app-bundled mobile-card pilot, individual credential operations and MCP removal |
| UX refinement | Core chat, requests, paging and recovery already implemented | Reproduce remaining scroll/IME/streaming-markdown/status issues before adopting new patterns; preserve theme/accessibility rules |
| F10 other agents | Research only | Separate Codex app-server / pi / ACP contract and authenticated remote-transport proofs, then one backend end-to-end |
| F7 local runtime | Current supported proot setup; partial native experiment | Genuine non-proot authentication/tool/SSE/rollback proof; no production replacement yet |
| F3–F6 supporting flows | Some existing Activity/notification/workspace primitives | Cross-server attention, handoff, digests and launch surfaces as separately scoped slices |
| E7 / E8 audience | English UI and experimental desktop builds | Continued externalization, reviewed translations/RTL, real desktop install/runtime checks |

Before release: perform the agreed final source/native/device gates, preserving
the recorded full-run plus focused-correction evidence, review the public
diff, then obtain publication approval. Broad tests stay deferred while this
development batch remains open.

## Corrections that change the plan

1. **Termux-native execution remains unproven.** Invoking bionic bash from
   inside proot does not leave proot. Version output and a 401 response are
   partial smoke evidence, not authenticated server/tool functionality. See
   the [corrected spike record](../verification/termux-native-server-spike-2026-09-06.md).
2. **Private address is not private transport.** Neither `100.64.0.0/10`, a
   `ts.net` name, nor an installed VPN app proves a particular route is
   encrypted. Keep HTTPS/loopback policy; prefer private HTTPS serving over a
   verified tailnet. Public Funnel exposure is a distinct opt-in decision.
3. **Subscription quota is not local spend.** Provider account-wide windows,
   OpenCode usage, and user budgets have different scopes and units. No
   token-to-subscription-percentage conversion. An unavailable quota is unknown,
   never zero remaining or unlimited.
4. **Codex app-server, pi RPC, and ACP are not interchangeable.** Capture each
   protocol independently. RUN_COMMAND is not a duplex stream. A small remote
   companion may be appropriate for CLI-only backends; Termux is not required
   for computer-hosted use or iOS remote control.
5. **Copy patterns, not unsupported guarantees.** A late failure must not revive
   a resolved approval; no optimistic approve-then-rollback or invented SSE
   replay. Performance changes require our own trace and benchmark.
6. **Research is not demand measurement.** Stars/downloads are not active users
   or causal growth evidence. No telemetry does not prohibit consented user
   observations, support feedback, or aggregate release-download evidence.
   Earlier numerical ICE guesses are withdrawn rather than treated as facts.

## Execution order

Android remains primary. Keep one product integration slice active, plus
disjoint research or native preparation. Shared-file edits serialize even
when the user outcomes belong to different epics. Product development does
not wait for an open-ended release-mechanics campaign.

| Order | Slice | User outcome / completion evidence | Dependencies |
|---|---|---|---|
| 0 | Preserve the implemented batch; verification recorded | Pin checks passed; the broad run's sole UI assertion failure was corrected and passed a focused rerun; native/live gates remain | No restart, credential access or release operation |
| 1 | **E13: extend remaining-usage coverage** | Codex is implemented; Claude collection is unavailable. Add providers only after supported/permitted integration and payload/unit/reset/auth evidence, then verify an explicitly authorized deployment | Separate GLM/MiniMax/Gemini adapters; do not substitute manual budgets for quota |
| Parallel prep | **E12: iOS remote-client build path** | Target scaffold/capability tests, then unsigned simulator/native compile on macOS; paste-connect, stream, approve, background/resume refetch | macOS/Xcode for native build; paid Apple enrollment only for TestFlight/distribution |
| 2 | **E5 + observed E2 regressions** | Stash migration preserves old data on failure and cannot collide with draft garbage collection; fix reproduced input/routing failures | One state/chat owner; migration and deletion fixtures |
| 3 | **F1a: read aloud**, then **F1b: voice conversation** | Foreground opt-in playback with safe audio interruption; then listen/review/send/listen loop with explicit confirmation and request identity | Engine/privacy/audio-focus proof; no voice-only tool approvals in the first slice |
| 4 | **F8 demo + F9 secure connection guidance** | First-run demo with no real network/profile side effects; tested private-HTTPS/tunnel instructions with actual SSE recovery | Reuse gateways without adding a protocol flavor; no CGNAT HTTP exception |
| 5 | **F2 mobile plugin surfaces**, E3/E4 provider/MCP completion | Read-only plugin inventory, then one app-bundled declarative card/action pilot; precise account and MCP actions | Real metadata or explicit reviewed mapping; no heuristic plugin identity or downloaded executable UI |
| Research lane | **F10 alternate agents**, F7 native-server experiment | One contract and topology decision per backend; clean non-proot test with real shell/Git/auth/SSE coverage | Separate Codex/pi/ACP evidence; no production migration from a successful `--version` |

E6 web sources, F3 cross-server attention, F4 handoff, F5 digests, F6 launch
surfaces, E7 localization, and E8 desktop stay in scope. Expand their stories
when pulled; they are not cancelled for lack of interviews. New copy uses ARB
now. Protocol parity and accessible mobile workflows are recurring gates.

## Acceptance rules for every pulled story

- State **current / missing / unknown**, the affected user job, input/output
  contract, capability gate, and origin scope (profile/location/session/request).
- Include success, first-load failure, stale refresh, cancellation, reconnect,
  and scope replacement where relevant. Acceptance may need multiple scenarios;
  one Gherkin pair is not a repository rule.
- Reuse shipped views before adding chrome. Review 360dp with keyboard and
  2.5x text, 48dp targets, reduced motion, TalkBack, and theme roles.
- Keep observable timing/cost estimates distinct from provider quotas. Retain
  unavailable/unsupported/unknown states and timestamps, not reassuring guesses.
- No third-party code copied without source commit, license/attribution review,
  maintenance-cost estimate, and native/dependency delivery assessment.
- Lead owns the final diff, analyzer and serial full suite. Focused checks run
  serially too. Native/signing/device/publication actions retain approval gates.

## Read-only review ownership and outstanding verification

Base: `db79069` plus this working tree. Protected: the existing pin/changelog
diff, planning edits, and all local research. Every reviewer has **write set:
none** for review tasks; implementation grants are separate and file-exclusive.
Reviewers must not install, run tests, access credentials, stage/commit or
publish. The initial specialist wave informed the implementation. A later
detached CLI experiment exited but denied source reads; its iOS/security
packets are not completed reviews. Public web findings remain unverified leads.

Prefer native server backgrounding (already supported by the app for eligible
tasks) over nested CLI workers. Tool exposure and server capability are separate;
do not infer either from a completed process. No new CLI workers are scheduled.

| Specialist | Read set | Dependency | Acceptance / focused check to recommend |
|---|---|---|---|
| `protocol-parity` | `lib/domain/`, `lib/api/`, `lib/api2/`, pinned contracts; F10/E13 source references | No implementation dependency | Distinguish native RPC from ACP; separate usage from quota; give method/event/schema/version and a sanitized fixture plan; name relevant protocol tests |
| `state-security` | `profiles.dart`, `connection.dart`, draft/stash stores, external-link policy, PRIVACY/SECURITY, E13/F9 | Proposed quota/connectivity boundaries | Threat-model routing, collector auth, secrets, expiry and deletion; recommend scope/failure tests (including profile deletion and offline queue) |
| `mobile-product` | complete chat library, usage/Activity/connection views, theme and shared widgets, E13/F1/F8 | Existing public gateway surface; no new contracts assumed | Story-state diagrams, no duplicate features, token/component reuse, compact/keyboard layout checks |
| `platform-runtime` | native/Dart channel pairs, platform gates, voice, Termux, manifests/dependencies, E12/F1/F7 | E12 remote-only scope; no live runtime access | iOS plugin/build matrix, audio-focus/lifecycle feasibility, valid non-proot experiment; propose platform/Termux/voice checks |
| `test-accessibility` | tests, QA records, l10n ratchet, all proposed UI stories | Current code plus lead plan; read-only throughout | Rank uncovered regressions vs existing coverage; specify fixture, 2.5x/TalkBack/reduced-motion and exact serial test plan |
| `milestone-delivery` | git diff, CI, release scripts, pubspec, release/readiness docs | Product slice boundaries, not a release request | Separate source/artifact/runtime evidence; iOS unsigned-build vs signing prerequisites; patch/baseline classification after diff stabilizes |

Required handoff: finding ID; exact source commit/path; fact/inference/unknown;
current vs proposed behavior; severity; smallest correction; dependency;
focused test; residual risk. Research recommendations are advisory; lead
accepts/rejects them explicitly. Repeated reviews must investigate unresolved
questions rather than re-summarize the same repositories.

## Approval and privacy boundaries

- No commit rewrite or git identity change is needed to continue product work;
  both require a separate instruction. Stay on `dev`; no automatic promotion.
- Private notes stay local, explicitly excluded from staging. An `internal/`
  directory name or an untracked status alone does not make a file private.
- Full source gates precede a requested commit/release. A signed build,
  TestFlight upload, push, tag, or credential use requires explicit approval.
