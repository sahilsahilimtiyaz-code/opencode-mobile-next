# Mobile experience: Portfolio C

Each row is a complete user journey. Proposed work is not enabled UI. Return brief, corrected F5 results and context capsules are now integrated locally after independent source review. The combined release gate and installed Android walkthrough remain pending.

| Idea / state | Evidence and finish line | Next branch | Limits / blockers |
| --- | --- | --- | --- |
| Return brief / implemented and integrated locally | Existing unread session state, pending requests and the separate F5 results screen. Returning to Workspace gives a compact unreviewed-work card with Review results, Answer and Continue. Explicit dismissal acknowledges only displayed identities and survives restart/profile deletion. | `feature/return-brief` at `81cfa89`, plus integration confirmation-retirement fix | Inventory can be partial and read state unsupported. No inferred absence interval, fabricated outcome, background refresh or automatic reply. Nested form confirmations retire with the request; final integration checks remain pending. |
| Phone context tools / context capsule implemented and focused-verified | Existing chat file picker, attachment budgets and draft vault. Chat Add → Context capsule collects labeled notes/errors/code and supported screenshots; users edit, preview/remove, then explicitly apply to the named task's existing draft. Cancellation leaves it unchanged. | `feature/mobile-context-actions` at `f89e7b1`, independent source review passed | First slice uses the existing native file picker, not new camera/share intake. Original profile/location/session and draft are checked across asynchronous operations. No downloaded executable UI, provider calls, automatic sends or background clipboard reading. Broader task/review shortcuts should open existing authoritative surfaces and coordinate with voice/development-server work. |
| Richer recorded GenUI cards / proposed | Existing mobile tool/task views, recorded command/file metadata, shared form renderer and separate F5 result evidence. A command/file/form card shows confirmed state, expands recorded output and works after history reload. | `feature/recorded-evidence-cards` | Start with command evidence and recorded exit metadata. Missing metadata stays unknown; prose cannot imply pass/fail. Forms retain current request identity and gateway checks. No AI summary or remote executable UI. |
| Premium visual consistency / proposed | Existing theme, framework motion, semantic icons and capture tooling. Workspace, brief and review surfaces share typography, spacing, controls, restrained motion and existing haptics that confirm explicit actions. | `feature/mobile-visual-consistency` | Verify light/dark, narrow phone, large text, RTL and reduced motion. Original design; no copied Apple assets or new animation package. Icon-pack picker remains deferred until a concrete recognition/accessibility need outweighs consistent meanings. |
| Preview-to-fix / proposed | Existing project preview/development-service surfaces and this reviewed context bundle are candidate building blocks. Open a project's preview, mark a visible issue, review a screenshot plus selected bounded logs/context, prepare a scoped fix request, then compare before/after with recorded check evidence. | `feature/preview-to-fix`, after capture feasibility | Choose the exact capture/annotation mechanism only after verifying current platform and gateway contracts. No arbitrary DOM injection, silent logs or secret collection, automatic send, or invented check results. Not part of this branch. |
| Reviewed task recipes / proposed | A small app-owned set, such as reproduce a bug, check accessibility or prepare a preview. Review visible inputs, target scope and expected evidence before explicitly executing the selected workflow. | `feature/reviewed-task-recipes`, after mobile context/tool contract feasibility | Build on existing gateway and context surfaces. No downloaded executable UI, opaque plugin permissions or automatic send. Keep execution and recorded evidence distinct; not part of the capsule branch. |

## Context capsule contract

- Entry: Chat's existing Add menu. A capsule is a working copy for the currently named task, not another inbox or attachment store.
- Add up to eight labeled excerpts: Note, Error and Code are starter labels, freely editable. Clipboard text is read only on explicit Paste. Each excerpt is bounded to 16,000 characters; the rendered bundle to 32,000.
- The existing file picker supplies supported images. Its five-file, 10 MB per-file and 20 MB aggregate limits include attachments already in the composer. Unsupported attachment capability leaves text useful and explains the limitation.
- Preview, edit and removal affect only the working copy. Back/Cancel discard unapplied content. It is transient while open; recovery of an unapplied capsule after process death is not claimed.
- Apply appends labeled text and selected images to the original draft without sending or replacing its existing contents. A changed owner, location, session, route or draft refuses application. Scope invalidation is permanent even if the connection returns to the old location.
- Applied content uses existing session-draft text and attachment references/blobs. Restart, write-refusal handling and profile deletion follow that existing storage path; no migration or new preference key.
- The ordinary composer save warning remains authoritative if persistence fails. An applied-but-unsaved draft is not described as saved.

## Verification

Feature tests, affected composer/draft checks and actual widget captures are tracked in [the capsule QA note](../qa/context-capsule-2026-09-08/README.md). Analyzer is clean; 58 feature/affected checks and five capture cases passed, with ten PNGs inspected. Native picker/device behavior and the full integration gate remain separate from synthetic widget evidence.

## Return brief contract

- Entry: below Workspace's project/context header, before its session sections. It reuses Activity's exact resolvers and F5 results; no new tab or inbox.
- Results are unreviewed idle **sessions**, not proof of successful runs or phone-observed run endings. The user opens results for the recorded outcome. No "since last visit" timestamp is inferred, including after restart.
- First use has no acknowledgements. Unsupported read state, incomplete session inventory and last-observed offline/request-refresh state are stated separately from a confirmed empty brief.
- Up to three result rows and three request rows are displayed. Overflow remains unacknowledged. Dismissing a page may reveal the next page of items. Session/request state is never changed by rendering, dismissing or opening results.
- Persistence: `oc.returnBrief.<profileId>`, keyed by `[baseUrl, directory, workspace]`. Exact `(sessionID, idle)` pairs and `[sessionID, request kind, requestID]` identities only; no transcripts, request bodies or credentials. At most 64 locations, 64 result pairs and 32 request identities per location. Eviction shows more work again; it never blanket-acknowledges unseen work.
- Save-before-hide: the saved set comes from the displayed snapshot, serialized with prior writes. A refused write leaves the card visible with retry. Profile deletion drains pending writes, uses the existing per-profile sweep and forgets the in-memory cache.
- Action callbacks retain profile/location scope. Result routes retire on scope change; permission/question use existing request guards. Form routes now retire on request/scope change and controller sends recheck after transport recovery.

## Return brief branch evidence

Focused domain, store, Workspace navigation and large-text tests are in `test/return_brief*_test.dart`; synthetic captures and exact check results are recorded in [the QA note](../qa/return-brief-2026-09-08/README.md). The before fixture removes the inserted brief from the same actual Workspace tree, reconstructing the baseline placement; it is not a screenshot of a deployed older app. After captures render implemented UI, not a proposed mockup. Final analyzer is clean; 133 distinct focused checks passed, with the affected 15 widget checks and six capture cases rerun after the final title-wrap change. Independent source review passed after the nested-dismiss correction; its focused execution and the full combined repository gate remain pending.
