# Quiet Workspace

Finish line: the selected project and recent conversations lead the page; full paths and honest review-state limitations remain accessible, and session actions retain their capability gates. Non-goal: protocol, storage, session grouping or project-management behavior changes.

The latest-dev ADB baseline showed a large wrapped path and a two-line technical read-state notice competing with the project and session title. Project names now use the title hierarchy, paths use one secondary line with selectable full text in context/details, and “Review status unknown” opens the complete existing explanation. All sessions uses the established placeholder-title presentation in its Continue-here confirmation without changing the stored title. Existing primary New session and isolated-task availability are retained; the action tray loses its competing outline and shadow above the navigation dock. At large text, Manage sits below the path so it cannot squeeze the project name.

Verified locally with Shorebird Flutter 3.47.2 (`e16cf749ccaa38d7050335ff305def49b1c7c84c`) on `design/quiet-workspace`, based on `395f374`: `flutter pub get`, one `flutter gen-l10n`, changed-file Dart formatting and `git diff --check` succeeded. `flutter test --no-pub --concurrency=1 --reporter expanded test/workspace_hierarchy_test.dart test/return_brief_widget_test.dart test/global_sessions_screen_test.dart tool/capture/quiet_workspace_test.dart` passed 57 tests. PNG inspection then exposed a project-name wrap at 2x text; after the responsive header correction, `workspace_hierarchy_test.dart` and `quiet_workspace_test.dart` were rerun serially and all 16 passed. The other two test files' implementation was unchanged. Logs: `/tmp/quiet-workspace-tests.log` and `/tmp/quiet-workspace-retest.log`.

Four synthetic phone captures in `docs/qa/quiet-workspace/` cover light/dark at normal and 2x text. Visual inspection confirmed the revised project heading, secondary path, status detail entry and primary action remain legible; these captures use the branch's base theme/navigation and are separate from live-device evidence. Integration analyzer/full-suite and final on-device validation remain coordinator gates. No native build, signing, push, CI or release was performed in this slice.
