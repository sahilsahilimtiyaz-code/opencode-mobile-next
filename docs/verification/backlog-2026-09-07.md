# Backlog implementation batch — 2026-09-07

Base: `cc57491`, direct `dev` work. No native workflow, signing, provider
credential access, collector deployment, live-server mutation or release.
Toolchain remains local Flutter 3.47.2 / Dart 3.13.2; this installation is not a
verified Shorebird fork. See the [current ledger](../backlog/current-state-2026-09-07.md)
for exact story dispositions and work still open.

## Focused verification

- Managed health + quota parser/client/UI: **69 passed, 1 optional preview
  skipped** (`/tmp/backlog-quota-health-tests.log`). A new test initially used
  the wrong bridge argument key; corrected to the existing `script` contract.
- Plugin mapper/screen and existing v2 event/mapper checks, plus light/dark
  capture harness: **44 passed** (`/tmp/backlog-plugin-tests.log`).
  [Dark](../qa/plugins/dark.png) and [light](../qa/plugins/light.png) captures
  use synthetic metadata through the real screen; no plugin code executes.
- Collector synthetic suite: **45 passed**, including missing-reset drift
  cases (`/tmp/backlog-collector-final.log`).
- Handoff/capture first compile caught explicit optional-facet cast and demo
  constant-constructor errors. These were corrected; **8 handoff tests passed**,
  including the added numeric-loopback spellings. Two health captures and the
  localization ratchet also passed.
- The real demo exposed a reduced-motion layout bug: four zero-duration size
  animations now render their final layout directly. The five initial isolation
  cases passed after correcting the test's platform-channel codec. The full six-case
  file passed after bounding compact input height. The final extended 360dp /
  2.5x / 320dp-keyboard check also passed through Send, reachable Allow once,
  completion and Exit. Setup CTA now requires a finished simulation and a
  dismissed keyboard; pending permissions no longer display it.
  Logs: `/tmp/demo-isolation-final-file.log`,
  `/tmp/demo-compact-centered-final.log`.
- Generated SDK: **analyzer clean, 47 tests passed**
  (`/tmp/backlog-sdk-analyze.log`, `/tmp/backlog-sdk-tests.log`).
- Final demo and handoff captures: **4 passed**
  (`/tmp/backlog-final-captures.log`). Root visually inspected the completed dark
  demo and light handoff preview. Captures use synthetic data through real UI:
  [demo](../qa/demo/complete-dark.png), [handoff](../qa/handoff/light.png),
  [managed health](../qa/managed-health/light.png).
- `flutter pub get`: passed (`/tmp/backlog-pub-get.log`).
- App analyzer: **clean** (`/tmp/backlog-app-analyze-final.log`). Initial
  analysis identified ten style issues and a capture's test-only preference
  API; fixed without ignores. The capture now uses route-owned memory
  preferences and its two theme cases passed again.
- Complete serial suite: **1,942 passed, 2 optional previews skipped,
  zero failures**, across all 197 files including nested goldens, at `cb10259`.
  See the final gate below and its checked-in manifest/results.

## Scope, privacy, accessibility and persistence

Plugin inventory retains only bounded IDs, validated package names, source
kinds, status and the declared terminal UI flag. Raw loader errors, source
paths/URLs and options are excluded. Rows are flat, status has text plus icons,
and enlarged-text behavior has focused coverage.

MiniMax uses the provider-documented Subscription Key endpoint, pinned official
CLI field semantics, explicit file configuration, fixed vendor host and
source-bound identity. Only reported general-pool percentages are shown.
Required timestamp drift is invalid, unlimited/boosted/absent measurements are
not fabricated, and PAYG keys do not become subscription quota. The phone sees
no provider key. [Collector sources and deployment requirements](../../tool/quota/README.md).

Health checks call the existing status command only. That command can reconcile
stale manager bookkeeping, but never starts or restarts a server. The view
labels its observation time and does not equate a live process with verified
authenticated server health or guaranteed overnight lifetime.

Handoff syntax was inspected from the installed CLI with `opencode attach
--help` (no server launched). The command is POSIX-shell quoted, excludes the
password, and requires a reachable HTTPS server. Clipboard confirmation binds
the reviewed session, project, directory, workspace and rebuilt command.

The demo owns its gateway, event channel, controller, preferences, profile
identity and handoff store. Isolation suppresses network transport construction,
native background handler registration, widget publication and routes/actions
that could escape the simulation. It does not replace global preference or
platform singletons. No persistent format migration is introduced by these
new slices.

## Initial full-manifest attempt

Candidate `9d9eb05`, fingerprint
`342968dec1323dc0e19c314867ee067b0d7ad013382abd3fdb7e3d867bcb16c2`:
`python3 /tmp/session-recovery-run-suite.py --candidate <repository> --output
/tmp/backlog-full-suite`. Chunks 1–3 passed (370 tests, one optional skip).
Chunk 4 stopped with one failure in `composer_layout_test.dart`: its Clear text
lookup existed below the viewport, so the test tapped at y=1008 on an 844px
screen. No full-suite pass is claimed. Correct the reachable-action exercise
and start a fresh candidate ledger; do not combine this attempt with a changed
snapshot.

The fixture now scrolls the real tools sheet and requires Clear text to be
hit-testable before tapping. The exact regression and all **17 composer layout
tests passed** (`/tmp/composer-clear-focused.log`,
`/tmp/composer-layout-final-file.log`). Product code did not change.

## Complete diagnostic sweep

Candidate `4bc96b1`, fingerprint
`528e8750ecdd13680e2aebc067b937fd3f6f45251bb48f4708f53b2559db71b1`:
all **197 files in 17 serial chunks** were inspected unchanged. After chunk 5
failed, `/tmp/complete-manifest-diagnostics.py` continued pending chunks while
retaining failed results and the same fingerprint. The sweep finished **failed**,
with six failed tests in five files:

- `desktop_platform_gating_test.dart`: missing localization delegates.
- `first_run_welcome_test.dart`: offscreen Guide action.
- `release_blockers_test.dart`: destination fixture lacks the active profile and
  matching session-details contract required by navigation guards.
- `session_relations_screen_test.dart` (two cases): no active saved profile.
- `voice_composer_test.dart`: voice fixture lacks the saved readable profile required before capture.

Ledger and per-chunk logs: `/tmp/backlog-full-suite-candidate2/`. The next
candidate requires a fresh complete gate after these focused fixes. App analysis
was clean before this sweep (`/tmp/backlog-app-analyze-candidate2.log`).

All five fixtures were corrected without production changes. They now exercise
visible scrollable actions, supply localization, and seed profile/session data
required by existing guards. Voice lifecycle coverage additionally proves
recording started before checking cancellation. **All 57 tests in these five
files passed** (`/tmp/backlog-fixture-fixes.log`).

## Final stable source gate

Candidate `cb1025937411c7ffd7f31b0034d7190eda224f45` passed the complete recursive manifest:
**1,942 tests passed, 2 optional previews skipped, zero failures**
across **197 files in 17 serial chunks**. Every chunk checked the same snapshot
fingerprint `7b2df7fc3be9889800d1be8bfaf475c48bff4901f75cf49943bd674bfab11d91` before and after running.
[Portable manifest, commands and chunk results](backlog-serial-gate-2026-09-07.json).
Full local ledger/logs: `/tmp/backlog-full-suite-final/`. App analysis was clean
(`/tmp/backlog-app-analyze-candidate3.log`); generated SDK analysis and 47 tests
and collector's 45 tests also passed as recorded above.

The skipped previews are optional screenshot cases in `active_context_test.dart`
and `provider_quota_screen_test.dart`; behavior tests were not skipped. Focused
new-flow screenshots were generated separately and are linked above.

Only this verification document and its result artifact are added after the
tested revision. These source gates do not establish native compilation,
physical-device behavior, account deployment, signing or publication. No CI run
or APK is part of this batch. Wider source work remains in the current ledger.
