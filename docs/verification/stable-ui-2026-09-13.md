# Android UI stabilization — 13 September 2026

Finish line: a readable resume → compose → monitor → unblock → review journey,
with preserved input and a reproducible local release gate.
Non-goal: adding features during stabilization.

## Candidate and ownership

Base: `5e3b8a4`, version `1.0.42+47`. Integration branch:
`fix/stable-ui-20260913`. The original `dev` checkout contains unrelated,
uncommitted AI Team work and is preserved. No push, native workflow, signing,
tag, or publication is authorized by this source stabilization task.

Four Claude Code workers use `claude-fable-5-1`, each in its own worktree:

| Slice | Write ownership | Acceptance |
|---|---|---|
| Workspace | Workspace/home screens and focused tests/capture | Project and session actions remain readable and usable at narrow widths with enlarged text |
| Chat | Chat screen and its part library, focused tests/capture | Conversation identity and compose/send/stop remain usable with keyboard and enlarged text; drafts and task access preserved |
| AI Team | Team home layout and focused tests | Filters remain reachable without displacing run status at large text |
| Release gates | Android quality workflow, serial runner, Python tests, slice evidence | Recursive test discovery and honest bounded/resumable coverage |

Workers preserve public method contracts and reuse localized strings. The
coordinator owns integration, visual review, and serial Flutter/native checks.

## Fresh baseline findings

These are current-source Flutter widget renders with synthetic data, captured
in this run. They establish layout evidence, not live-server behavior.

1. **Workspace, ordinary text — usable.** Session groups and the primary action
   are visible. [Capture](../qa/stable-ui-2026-09-13/before/01-workspace-dark.png).
2. **Workspace, 320 logical pixels / 2.5× text — needs repair.** The project name
   breaks mid-word, the primary action reads “New ses…”, and session state loses
   useful width. [Capture](../qa/stable-ui-2026-09-13/before/02-workspace-large-text.png).
3. **Chat draft — needs header/layout review.** The Tasks label competes with
   conversation identity; keyboard and large-text behavior need explicit
   coverage. [Capture](../qa/stable-ui-2026-09-13/before/03-chat-draft.png).
4. **First run — usable in the captured states.** Connect, demo, and phone setup
   are discoverable. [Capture](../qa/stable-ui-2026-09-13/before/04-first-run.png).
5. **More — usable at ordinary text.** Search and grouped navigation are visible;
   scrolling and accessibility need behavior checks beyond this image.
   [Capture](../qa/stable-ui-2026-09-13/before/05-more.png).

Do not infer TalkBack compliance from these screenshots.

## Baseline checks

Pinned Flutter: `3.47.2`, cache revision
`e16cf749ccaa38d7050335ff305def49b1c7c84c`.

- `flutter pub get`: passed.
- `flutter analyze --no-pub`: clean.
- Serial `tool/capture/fluid_shell_test.dart`, `quiet_chat_test.dart`, and
  `simple_first_run_test.dart`: 20 passed; light/dark and enlarged-text captures.
- Generated SDK: dependency lock enforced, clean analysis, 47 tests passed.
- Four SDK contract/generated/matrix/independent integrity verifiers: passed.
- `bash test/release_script_test.sh`: passed.

These baseline app checks do not replace final candidate validation after UI
changes. SDK integrity checks apply while the SDK/contracts remain unchanged.

## Release state observed now

GitHub API inspection returned eight releases, all prereleases; no stable
release exists. The latest listed development APK is `dev-fad2875`. Existing
readiness documentation describes older source and is not current proof.

The local x86_64 APK was installed only on the isolated Pixel 6 emulator for a
baseline demo smoke. Welcome → demo → prompt → permission review → allow once →
simulated completion worked with the keyboard visible. It uses a debug
certificate (`1de5bf08…978a62494d60c`), not the required maintainer CI certificate.
Its source provenance is not verified; it is not a candidate or upgrade proof.
No signing key was read or used.

That artifact declares package `io.github.eslamasabry.opencode_mobile`, version
`1.0.42`, Android versionCode `4047` (distinct from pubspec build 47), SHA-256
`f24e3540d2a5e476d48b8dad716b239c59a146c7405a55559f0fafba08f5d059`.

The release script now uses the same recursive serial runner, with 25-file
chunks and a 900-second deadline. Its safety fixture includes a nested test
and proves a failed gate prevents the Shorebird command; the updated shell
contract passes. `HANDOVER.md` now labels the earlier concurrency-4 run as
historical instead of instructing future agents to bypass the serial gate.

## Final candidate gate

Candidate source: `935b1f5` on `fix/stable-ui-20260913`.

- Changed Dart files formatted; diff whitespace check clean.
- Final analyzer: no issues.
- Final chat layout/composer/capture batch: 35 passed. Workspace: ten behavior tests and ten capture cases passed. Team: affected behavior/layout tests and all three new font-aware cases passed; two fresh capture cases passed.
- Python release runner checks: 21 passed. Release script safety contract passed.
- First recursive serial attempt: ten chunks passed, then chunk 11 found four fixture-registry failures (three committed recording types lacked paths). The registry was corrected; all 97 mapper tests passed. This attempt is failed, not a full pass. Evidence is in `build/traycer/serial-20260913T041305Z-8d8d3b42`, 13 chunks of at most 25 files. Initial source fingerprint `a2a9f4eb12e05103280af8a55b649e202ff9ad785a04d2f3b53dd28ba34f5895`.
- Android release compile: Kotlin `MainActivity.class` and Termux bridge classes were generated; reached `:app:validateSigningRelease` and stopped because no keystore is configured (462.8 seconds). Dart AOT `app.so` and merged native libraries were produced; no signed APK was produced. SDK API 37 is installed. The available Java is OpenJDK 17.0.20 (Ubuntu), not the requested Temurin vendor. No signing configuration was copied.

Visual review used fresh synthetic widget captures for ordinary/enlarged text in dark and light themes. Native demo baseline was exercised separately on an emulator using the older APK identified above. No installed candidate, native TalkBack, live-server regression, signed upgrade, or public stable release is claimed.

The local source candidate still requires the maintainer-approved Android quality workflow, certificate verification against the installed maintainer signer, and install/upgrade smoke before delivery. Publication is a separate explicit decision. The public historical releases also used different certificates; compatibility across public release lineages remains a release decision, not something this UI branch changes.

## Completed final gate

The unchanged `935b1f5` source passed all 13 serial chunks: **324 files, 3,753 passed, 12 skipped, zero failures**, in 800.73 seconds of chunk execution. This fresh run supersedes the failed first attempt; no coverage from the earlier snapshot is included.

- Run: `build/traycer/serial-20260913T043314Z-e1e8e6ba`.
- Source fingerprint: `b2f46ee2dc95017980088c935852e7f7df9282c88495eececcb5a215cf2dbc89`.
- [Summary](stable-ui-2026-09-13/summary.json), [commands and chunks](stable-ui-2026-09-13/run.json), [complete recursive test manifest](stable-ui-2026-09-13/test-manifest.json), [source manifest](stable-ui-2026-09-13/source-manifest.json). Logs remain in the run directory; the summary's log paths are relative to that directory.
- The nested theme gallery golden file passed. Twelve opt-in preview/capture cases were skipped: active context (1), appearance (4), chat background result (1), completion digest (1), E7 workspace capture (1), provider quota preview (1), voice reply previews (3). No functional failure was converted into a skip. The new UI capture harnesses were run separately and reviewed.
- Final analyzer: clean after the fixture registry repair. No SDK or contract source changed from the verified 47-test SDK baseline.
- Local Android compile stopped only at the absent signing configuration. No signed APK, installed candidate, or public release exists from this branch.
- Task-owned emulator closed after the baseline smoke; the task-created idle Gradle daemon was stopped by its exact PID.

Priority: deliver this UI stabilization candidate before broader Gas City feature work. Only the missing fixture registry entries were brought into this candidate from the in-progress area. Original `dev` remains at `5e3b8a4` with its unrelated uncommitted work intact. This branch is locally committed, not pushed or merged.

Next release gate: explicit maintainer approval to push the candidate and run the Android quality workflow using the existing maintainer CI signer; then verify certificate, installation and in-place upgrade. Public stable promotion still requires the normal release approval and branch preconditions.

## Authorized release continuation

The maintainer explicitly authorized signing and publication on 13 September
2026. GitHub rejected the original local history because it contained an
archived 180 MB APK. Release branch `release/stable-1.0.42`, snapshot `c900bef`,
uses the exact tested app/source tree on remote dev history; only that archived
APK was removed from tracking and an APK ignore rule added. Existing local
checkouts/history remain intact. Both public and maintainer signers are preserved.

Native public upgrade testing exposed a real blocker before publication: a
healthy OpenCode 2 endpoint may omit version metadata, while the root, share
and launcher routes incorrectly required a non-null version. The app stayed
on Connecting after cold start. Readiness now follows the initialized healthy
gateway, survives SSE reconnection and resets when that gateway is retired.
Mounting the root also leaves a connection already in flight alone. Unknown
version metadata remains unknown.

The corrected source passed 73 focused routing/connection/location tests and
a clean analyzer. Earlier full-suite evidence applies to its earlier snapshot,
not this change. A new full CI gate and both signed builds are required, followed
by native install/upgrade and cold-start checks. Nothing has been published.

The first full CI run passed its Flutter suite but stopped on two Android lint
errors. The Quick Settings launch now uses AndroidX's compatible immutable
PendingIntent wrapper, and the Arabic tile plural includes its required zero
form. Local `:app:lintRelease` passes with no errors (existing warnings remain),
and all 11 native launch-surface contract tests pass. The next CI run must cover
this corrected candidate before either signed APK is delivered.
