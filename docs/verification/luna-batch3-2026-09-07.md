# Luna batch 3 verification — 2026-09-07

This reliability batch starts at `f866fd9f59276f62956339342459f882d2a7f138`
on the existing `dev` checkout tracking `mobile-next/dev`.
The [rolling ledger](../backlog/rolling-swarm-2026-09-07.md) records assignments,
review corrections and the maintainer's priority change toward major journeys.
These changes are not ten major new features. The next active milestone is an
integrated Codex connection and conversation flow, subject to supported transport
and authentication evidence.

## User outcomes and review

Queued input snapshots cannot be mutated by callers while persistence awaits.
Writes are ordered, and unreadable/wrongly typed saved queue data blocks silent
replacement. Settings explains unknown queue contents and offers confirmed
local deletion; Cancel preserves the saved data. Read-marker writes retain
unacknowledged state for retry. Budget loaders retain valid rules while exposing
partial corruption instead of silently declaring healthy storage.

Notes retain route-bound edits across connection changes. Import, saved
permission, related-session, file viewer and attachment operations retire old
asynchronous work after controller/location changes. File read errors allow
explicit Retry. Voice capture termination and model finalization cancellation
retire the current operation without disturbing a newer one. Global session
search reuses localized Clear and Retry labels.

Root rejected proposed pin/read limits that could discard valid legacy data,
and rejected a stash error thrown after an acknowledged save. No pin or stash
change is counted. Root corrected listener ownership, late completion guards,
stuck note-save state, protocol-neutral file access, partial budget warning truth,
and queue recovery availability. Android visual review caught an incorrect zero
queue count; the final candidate explicitly reports that the count is unknown.

Known remaining issue: offline flush can ignore a failed final persistence write
after server acceptance, leaving an older disk queue that could be resent after
restart. This batch does not claim exactly-once delivery. The next correction
requires durable uncertain-delivery review or a proven idempotency contract;
removing text before sending is not an acceptable substitute.

## Source evidence

The complete recursive serial suite passed **2,139 tests in 215 files**, with
three optional preview skips and zero failures across 11 chunks (460.57 seconds).
The [result manifest](luna-batch3-2026-09-07-results.json) lists every covered file.
Source fingerprint: `0fcc7d15e58609d403368a32027d95cb59fbd0771aa3329f2a8bcab3611af25b`.
The runner verified it before every chunk and after completion. Final analyzer
reports no issues; all 29 changed Dart files format without changes and
`git diff --check` passes. Generated SDK analyzer and **47 SDK tests** pass.
Final focused note/queue tests passed 46 checks and Settings passed 17; focused
runs overlap the recursive gate and are not added to its total.

Raw evidence is retained at
`/tmp/ocmn-swarm-batch3/`. Failed compile/fixture runs and two note-test hangs are
retained; root stopped the exact owned processes, corrected the unrebuilt Save
button fixture and bounded its write-start assertion. Failed/interrupted runs
are not counted as passing evidence.

## Android evidence

The final debug APK `1.0.36+37` builds with Flutter 3.47.2 / Dart 3.13.2,
JDK17 and compile API37. The toolchain is the `shorebirdtech/flutter` tag
`d3b14c876900e553bc736ca19295fc09e3853e8e` with the standard engine; this does
not validate the release-pinned Shorebird engine or updater. Final APK SHA-256:
`b3d7b5b2a931599f6883d13dcbc858bdc386e516991165c69c432f325ea3807a`.

The installed app was tested through ADB on `emulator-5554`, Android14 x86_64,
against a synthetic v1 fixture at loopback4125 using ADB reverse. Only the
isolated emulator queue preference was deliberately corrupted; no real account
or provider was accessed. The [Android result](luna-batch3-2026-09-07-android.json)
records these completed checks:

- Unreadable queue storage reports an unknown count at normal and 2.0 font scale.
- The clear confirmation explains permanent local deletion of unknown contents.
  Cancel preserves the raw saved bytes; confirmed Delete removes them.
- A synthetic file HTTP500 displays a recoverable error. Explicit Retry after
  recovery renders the requested README.
- No prompt was sent. Captured AndroidRuntime and Flutter error logs are empty.
  Font scale was restored to 1.0 and the final app remains installed.

Root visually inspected [large text](../qa/luna-batch3-2026-09-07/queue-unreadable-large.png),
[confirmation](../qa/luna-batch3-2026-09-07/queue-clear-confirmation.png), and
[recovered file](../qa/luna-batch3-2026-09-07/file-read-recovered.png). Text wraps
and remains scrollable; actions are explicit. These captures contain synthetic
data. This is local debug evidence, not physical-device, TalkBack, real voice,
Termux, authenticated model, signed upgrade, CI or release verification.

## Commit record

The verified source is committed through `db50c1fa23af1808c4fed723a88c01fe2f85506e` in ten
logical commits on existing `dev`, each with `[skip ci]`. This documentation
commit records the evidence without changing the source candidate. No CI or
signing workflow was dispatched.
